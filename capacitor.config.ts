import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'isbike_istasyon',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
